services:
  portfolio:
    image: nginx:alpine
    ports:
      - "8888:8888"
    volumes:
      - ./app:/usr/share/nginx/html:ro
      - ./default.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
    networks:
      - proxy
networks:
  proxy:
    external: true